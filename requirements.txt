# 浏览器由 Dockerfile 里的 `playwright install chromium` 提供，版本与本行
# 严格配套，所以这里钉死小版本即可，不必再迁就系统 chromium。
# 1.63.0 自带 chromium 153.0.8010.12。
playwright>=1.63.0,<1.64.0
beautifulsoup4>=4.15.0,<5.0.0
requests>=2.34.0,<3.0.0
pydantic>=2.13.0,<3.0.0
redis>=8.1.0,<9.0.0
pandas>=2.3.0,<3.0.0
msgpack>=1.2.0,<2.0.0
python-dotenv>=1.2.0,<2.0.0
# mcp 2.x 把 FastMCP 改名成 MCPServer 并换用 httpx2，是硬断裂；
# main.py 的 100+ 个 @mcp.tool() 迁移前保持 <2 上界。
mcp>=1.30.0,<2.0.0
email-validator>=2.3.0,<3.0.0
